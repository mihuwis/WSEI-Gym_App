<Window x:Class="GymApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymApp.Views"
        mc:Ignorable="d"
        Title="Login" Height="200" Width="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="Username" Margin="10,10,10,0"/>
            <TextBox Text="{Binding Username}" Margin="10,0,10,10"/>

            <TextBlock Text="Password" Margin="10,10,10,0"/>
            <PasswordBox Password="{Binding Password}" Margin="10,0,10,10"/>

            <Button Content="Login" Command="{Binding LoginCommand}" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
