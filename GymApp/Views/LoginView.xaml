<Window x:Class="GymApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymApp.Views"
        mc:Ignorable="d"
        Title="Login" Height="200" Width="300">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Username}" Margin="10" Width="250"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="10" Width="250" />
            <Button Content="Login" Command="{Binding LoginCommand}" Margin="10" Width="250"/>
        </StackPanel>
    </Grid>
</Window>
